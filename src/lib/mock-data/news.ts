import type { NewsPost } from "@/lib/types";

// Public news is loaded from the live CMS so existing articles and their
// images remain the single source of truth.
export const newsPosts: NewsPost[] = [];
