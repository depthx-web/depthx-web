import type { Publication } from "@/lib/types";

// No public research papers have been confirmed yet. Add only verifiable
// publications here so the website never implies peer review or conference
// acceptance that has not occurred.
export const publications: Publication[] = [];
